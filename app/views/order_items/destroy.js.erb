$("#order_item_<%=@item.id%>").remove();
$("#total").empty().html('<%= number_to_currency current_order.total_price %>')
$('.cart-quantity').empty().html('<%= j link_to "Cart (#{ cart_items })", cart_path %>');
